# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Sensitive files (but allow environment configs - no secrets)
*.tfvars
!example.tfvars
!terraform/environments/*/terraform.tfvars

# macOS
.DS_Store